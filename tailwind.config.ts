/* Tailwind config neutralized — Tailwind is not used by the migrated Node/EJS app.
   Keeping this file (neutral) avoids build tooling errors but it contains no active config.
*/

export default {};
